# REQ-14 — Seed constants

## Requirement (verbatim)

From `/home/david/claude/thugbirdz/IMPLEMENTATION_APPENDIX.md` §2, Seeds:

> pub const POOL_SEED:  &[u8] = b"pool";
> pub const VAULT_SEED: &[u8] = b"vault";
> pub const MAP_SEED:   &[u8] = b"map";
> pub const TREASURY_SEED: &[u8] = b"treasury";

Holds if: seed constants are exactly the byte strings `"pool"`, `"vault"`, `"map"`, `"treasury"`.

## Verdict

**implemented** — confidence **high**.

## What the requirement demands

Four `pub const` byte-string seed constants with exactly these names and exactly these
values, and (implicitly, for the requirement to matter) that PDA derivation throughout
the program actually uses these constants rather than divergent literals.

## Where enforcement lives

`program/programs/thugz-swap/src/constants.rs` L3-L6:

```rust
pub const POOL_SEED: &[u8] = b"pool";
pub const VAULT_SEED: &[u8] = b"vault";
pub const MAP_SEED: &[u8] = b"map";
pub const TREASURY_SEED: &[u8] = b"treasury";
```

Character-for-character identical to the appendix except for column-alignment
whitespace in the appendix (`POOL_SEED:  &[u8]` vs `POOL_SEED: &[u8]`), which has no
semantic content.

## Usage paths walked

Every PDA derivation in the program uses the constants, never a raw literal. The grep
for the raw byte-literals `b"pool"`, `b"vault"`, `b"map"`, `b"treasury"` across
`src/` and `tests/` hits ONLY `constants.rs` L3-L6 — no shadow literals anywhere.

Constant usage sites (all via the named constants):
- `POOL_SEED`: `swap.rs` L17, `deposit_bird.rs` L19, `fix_mapping.rs` L17,
  `recover.rs` L17, `seal.rs` L12, `set_paused.rs` L11, `initialize_pool.rs` L23.
- `VAULT_SEED`: `swap.rs` L48/L138, `deposit_bird.rs` L47, `fix_mapping.rs` L35/L91,
  `recover.rs` L33/L91, `initialize_pool.rs` L41.
- `MAP_SEED`: `swap.rs` L34, `deposit_bird.rs` L43/L108, `fix_mapping.rs` L26,
  `recover.rs` L24.
- `TREASURY_SEED`: `swap.rs` L69, `deposit_bird.rs` L58/L90, `fix_mapping.rs` L57,
  `recover.rs` L58, `initialize_pool.rs` L42, `instructions/mod.rs` L22/L44.

Tests import the same constants from the crate (`tests/level1.rs` L37,
`tests/level2_properties.rs` L41) and derive PDAs with them
(`level1.rs` L90-L92, L157-L158; `level2_properties.rs` L286-L288, L325-L326),
so the test suite and program cannot silently disagree on seed values.

## Searches performed

1. `grep -rn "SEED" src/` — 33 hits: the 4 definitions in `constants.rs` L3-L6 plus
   usage in every instruction file listed above. No fifth seed constant, no unused one.
2. `grep -rn 'b"pool"|b"vault"|b"map"|b"treasury"' src/ tests/` — 4 hits, all in
   `constants.rs` L3-L6. No divergent inline literals.
3. `grep -rn '"pool"|"vault"|"map"|"treasury"' tests/` — 0 hits outside constant
   imports (tests use the imported constants, not string literals).
4. `grep -rn "find_program_address|SEED" tests/` — all derivations use the imported
   constants.

## Why this verdict and not the adjacent one

- Not `partial`: no path derives a PDA with a different seed string; the raw-literal
  grep proves there is no second source of truth.
- Not `stronger-than-spec`: the code does exactly what the appendix prints — same
  four names, same four values, nothing extra in the seed namespace.
- Byte-string equality is verified by direct comparison of the source lines with the
  appendix text, not by trusting names.

## Open questions

None. The requirement is a literal source-text invariant and the source matches.
